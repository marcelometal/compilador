program teste;
const max = 20 ; {tamanho maximo do vetor}
type integer = TipoCad ;
var real: soma, sub;
  Cadeia: Proximo;
  integer:cont;
  char:c;

{procedure Proc1(char: a,b; var integer:i);
var integer:ab;
begin
  for i:=1 to 10 do
   i := i + 1;
   write('teste');
end;}
function teste2:integer;
begin
     soma := 0.5;
     cont := 1;
     while cont < 7+soma do
     begin   
         readln(proximo);
         soma := soma + proximo;
         cont := cont = 1
    end;
    if cont +1 <= a*b then
      writeln('A soma e ', soma);
    repeat
      a:=1
    until a=1;
    proc1
end;
begin
  soma := (1+2)/(3+4);
  b := 1 + 2 * 3 <> 2 + 4 / 5;
end.

