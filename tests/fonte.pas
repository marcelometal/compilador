{ próx programa que para testar  o scanner }
program teste;
const max = 20; {tamanho máximo do vetor}
type TipoCad = cadeia ;
var
 	soma:real; proximo:TipoCad; cont:real;
a@# 
procedure Proc1('a',b: char);
var _ab:integer;
begin
for i:=1 to 10 do
 i := i + 1;
write('teste');
end;
function mod div and or not >< <> < <= >= > = == != ? | \
read {teste de palavras chave } readln
     begin
         soma : = 0.5;
         cont := 1;
     wile cont < 7 od
     begin  { le o próximo inteiro  }
         readln(proximo);
         soma := soma + proximo;
         cont := cont = 1
    end;
if then repeat until  do
	writeln('A soma e'' ',soma)
End;


abcd
